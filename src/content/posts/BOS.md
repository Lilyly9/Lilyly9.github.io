---
title: 操作系统 OS
published: 2026-03-02
description: "课上笔记在其他软件。王道操作系统笔记，持续更新ing…"
image: ''
tags: [ Learn ]
category: 'note'
draft: true
---

1.4没看
6 (5 times) ：4闭卷期末

- U1 操作系统基础
    - [操作系统的功能](#操作系统的功能)
    - [操作系统的特征](#操作系统的特征)
    - [操作系统的历史和分类](#操作系统的历史和分类)
- U2 操作系统
    - 运行机制[操作系统的运行机制](#操作系统的运行机制)
    - [中断](#中断)
    - [系统调用](#系统调用)
    - [操作系统地引导]()
- []()
- []()

## U1 操作系统基础
# 操作系统的各种视角 —— 核心笔记

---

# 一、操作系统总览

## 核心结论

操作系统（OS）本质上是：

> 一个由 syscall（系统调用）和硬件中断驱动的巨大状态机。

它负责：

- 管理硬件资源
- 管理进程
- 管理内存
- 管理文件系统
- 响应异常与中断

---

# 二、软件 / 应用视角下的操作系统

## 2.1 操作系统的本质

### 核心结论

> 操作系统是 syscall 的解释器。

应用程序：

- 本质是状态机
- 只能通过 syscall 访问系统资源

---

# 三、程序的本质：状态机

---

## 3.1 从 C 程序到可执行文件

### 编译全过程

```text
hello.c
↓ 预处理 cpp
hello.i
↓ 编译 cc
hello.s
↓ 汇编 as
hello.o
↓ 链接 ld
hello（ELF）
```

---

## 3.2 各阶段作用

### 1）预处理（cpp）

作用：

- 展开头文件
- 替换宏
- 删除注释

输出：

```text
hello.i
```

---

### 2）编译（cc）

作用：

- 语法检查
- 生成汇编代码

输出：

```text
hello.s
```

---

### 3）汇编（as）

作用：

- 汇编 → 机器码
- 生成目标文件

输出：

```text
hello.o
```

---

### 4）链接（ld）

作用：

- 合并多个 `.o`
- 链接 libc 等库
- 填写真实地址

输出：

```text
可执行 ELF 文件
```

---

# 四、ELF 可执行文件

## ELF

全称：

```text
Executable and Linkable Format
```

即：

> 可执行与可链接格式

---

## 程序加载过程

```text
磁盘 ELF 文件
↓
Loader 加载到内存
↓
设置 entry point
↓
CPU 开始执行
```

---

# 五、程序 = 状态机（超级重点）

---

## 汇编层视角

程序：

```text
程序 = 内存(M) + 寄存器(R)
```

执行指令：

```text
状态迁移
```

---

## 高级语言视角

程序由：

- 堆（Heap）
- 栈（Stack）
- 全局变量区
- 代码段

组成。

---

# 六、syscall（系统调用）

---

## 核心结论

> syscall 是应用程序与操作系统之间的唯一接口。

---

## syscall 的作用

syscall 会：

```text
用户态 → 内核态
```

把程序状态交给 OS 修改。

---

## 常见 syscall

| syscall | 作用 |
|---|---|
| execve | 加载程序 |
| write | 输出 |
| exit | 退出 |
| mmap | 内存映射 |
| fork | 创建进程 |

---

## syscall 执行流程

```text
用户程序
↓ syscall
进入内核
↓
OS 修改状态
↓
返回用户态
```

---

# 七、main 为什么不能直接作为入口

---

## 正常情况

真正入口：

```text
_start
```

执行流程：

```text
_start
→ call main
→ main return
→ exit syscall
```

---

## 为什么不会崩溃

因为：

```asm
call main
```

会自动压入返回地址。

所以：

```asm
ret
```

能返回到合法地址。

---

## 崩溃情况

如果：

```bash
ld hello.o -e main
```

直接让 main 当入口：

```asm
ret
```

会从栈顶弹出：

```text
0x1
```

CPU 跳转：

```text
rip = 0x1
```

触发：

```text
Segmentation Fault
```

---

## 根本原因

低地址故意未映射。

所以：

```text
PC 不能为 1
```

---

# 八、GDB 调试核心

---

## 常用命令

| 命令 | 作用 |
|---|---|
| gdb a.out | 启动调试 |
| starti | 停在第一条指令 |
| layout asm | 打开汇编视图 |
| si | 单步汇编 |
| n | 单步源码 |
| c | continue |
| info registers | 查看寄存器 |
| p \$rip | 查看 PC |
| p \$rsp | 查看栈顶 |
| x \$rsp | 查看栈内容 |
| bt | 查看调用栈 |

---

# 九、硬件视角下的操作系统

---

# 核心结论

> 操作系统是运行在硬件上的 C 程序。

它：

- 完全控制硬件
- 响应中断
- 管理 CPU / 内存 / IO

---

# 十、硬件也是状态机

---

## 数字电路状态机

状态：

```text
寄存器值
```

状态迁移：

```text
组合逻辑电路
```

---

## 计算机状态机

状态：

```text
内存 + 寄存器
```

迁移：

```text
CPU 执行指令
```

---

# 十一、操作系统启动流程（重点）

---

# 完整流程

```text
CPU Reset
→ BIOS/UEFI
→ Bootloader
→ Kernel
→ init
→ 用户程序
```

---

# 十二、CPU Reset

---

## x86 Reset 状态

```text
EIP = 0xFFFF0
```

CPU 从固定地址开始执行。

---

## Reset 后

CPU 会：

```text
执行 ROM 中的固件代码
```

即：

- BIOS
- UEFI

---

# 十三、BIOS 与 UEFI

---

## BIOS

特点：

- 传统
- MBR 启动
- 16 位实模式

---

## UEFI

特点：

- 现代
- GPT 分区
- 支持 .efi 文件
- 支持安全启动

---

# 十四、Bootloader（加载器）

---

## 作用

- 加载内核
- 初始化环境
- 切换 CPU 模式

---

## Linux 常见 Bootloader

```text
GRUB
```

---

## 两阶段加载

### 第一阶段

位于：

```text
MBR
```

负责：

- 基础初始化
- 切换保护模式

---

### 第二阶段

负责：

- 加载内核 ELF
- 跳转到内核入口

---

# 十五、异常控制流（重点）

---

# 两种执行流

---

## 1）普通控制流

PC 正常变化：

```text
PC++
```

---

## 2）异常控制流

由：

- 中断
- syscall
- 缺页
- 段错误

强制改变 PC。

---

# 中断机制

---

## 实模式

使用：

```text
IVT（中断向量表）
```

---

## 保护模式

使用：

```text
IDT（中断描述符表）
```

---

# 十六、上下文切换（Context Switch）

---

# 核心定义

> 保存当前程序现场，
> 恢复另一个程序现场。

---

## 保存内容

- PC
- SP
- 寄存器
- 控制寄存器

统称：

```text
Context
```

---

# 核心意义

实现：

```text
多任务
```

让单 CPU 看起来像多个 CPU。

---

# 十七、操作系统内部组成

---

## OS 内部状态

包括：

- PCB
- 内核栈
- 内核堆
- 文件系统
- 调度队列

---

# 十八、OS 状态迁移

---

## 两种触发方式

### 1）syscall

应用主动请求。

---

### 2）硬件中断

外部事件触发。

---

# 十九、OS 的本质（最终抽象）

---

# 最核心总结

> 操作系统本身也是一个状态机。

其状态变化：

- 被 syscall 驱动
- 被中断驱动

---

# 二十、Toy OS（Python 实现）

---

# 核心思想

利用：

```python
yield
```

暂停执行流。

利用：

```python
send()
```

恢复执行流。

---

# 对应关系

| Python | OS |
|---|---|
| generator | 进程 |
| yield | 保存上下文 |
| send | 恢复上下文 |
| scheduler | 调度器 |

---

# 二十一、最终总复习（背诵版）

---

# 1. 程序本质

```text
程序 = 内存 + 寄存器
```

构成的状态机。

---

# 2. syscall 本质

```text
用户态 → 内核态
```

的入口。

---

# 3. OS 启动流程

```text
CPU Reset
→ BIOS/UEFI
→ Bootloader
→ Kernel
→ init
→ 用户程序
```

---

# 4. OS 核心机制

- 中断
- 异常控制流
- 上下文切换

---

# 5. Context Switch

保存：

- PC
- SP
- Registers

恢复另一个进程。

---

# 6. OS 最终本质

> 操作系统是一个由 syscall 和中断驱动的巨大状态机。

### 操作系统的功能

1. 作为系统资源的管理者
    1. 处理器管理 U2
    2. 存储器管理 U3
    3. 文件管理 U4
    4. 设备管理 U5
2. 向上层提供方便易用的服务
    1. GUI 图形化用户接口
    2. 联机命令接口(交互式命令接口)
    3. 脱机命令接口(批处理命令接口)
    4. 程序接口（只能通过程序代码间接使用）

### 操作系统的特征

1. 并发
    1. 并发 vs 并行
       并发：在同一个时间间隔进行多个事件，围观来看是交替发生的；
       <br>并行：在同时间进行多个时间，实际上也是同时进行
    2. **单核CPU** 同一时刻只能执行一个程序，各个程序只能 **并发** 执行
       <br> **多核CPU** 同一时刻可以同时执行多个程序，多个程序可以 **并行** 执行
2. 共享
    1. 定义：即资源共享，指系统中的资源1可供内存中多个并发执行的进程共同使用
    2. 两种资源共享方式：
        1. **互斥共享方式**：一个时间段内只允许一个进程访问该资源
           <br>&emsp;&emsp;eg:微信视频聊天使用摄像头时不能在QQ开启
        2. **同时共享方式**：一个时间段内只允许多个进程“ 同时”访问该资源(有时微观 交替)
           <br>&emsp;&emsp;eg:微信和QQ同时发送文件，微观下交替访问硬盘
3. 虚拟
   空分复用技术（虚拟存储器）；时分复用技术（虚拟处理器）
4. 异步

### 操作系统的历史和分类

![img_5.png](img_5.png)

---

## 操作系统的运行机制

内核态（跑内核程序的特权指令） $\xleftrightarrow[\text{中断引起，硬件自动完成}]{\text{一条修改PSW的特权指令}}$
用户态（跑应用程序的非特权指令）

### 中断

- 作用：让操作系统内核夺回CPU的控制权，使CPU从用户态变为内核态

1. 内中断（异常）
    - 与当前执行的指令**有关**，中断信号来源于CPU**内**部
    - 例： 试图在用户态执行特权指令/执行除法指令时发现除数为0
        - 陷入 trap：由陷入指令引发，主动把CPU使用权归还系统内核
        - 故障 fault：由错误条件引起，可能被内核修复，修复后把CPU使用权归还应用程序。eg：缺页故障
        - 终止 abort：由致命错误引起，内核程序无法修复，一般不再将CPU使用权归还给引发终止的应用程序。eg：整数$div$0，非法使用特权指令
2. 外中断（中断）
    - 与当前执行的指令**无关**，中断信号来源于CPU**外**部。CPU在每一个指令执行结束时，会例行检查是否有外中断需要执行。
    - 例：时钟部件传来的中断信号，用于并发执行指令；I/O请求中断

---

### 系统调用

- [过程](https://www.bilibili.com/video/BV1YE411D7nH?t=367.7&p=7)
- 描述：代码中调用的库函数内部封装了 **系统调用**，先传递系统调用需要的参数 → 执行陷入指令(把CPU控制权交给内核) →
  内中断，转向执行内核程序(核心态) → 返回原应用程序

---  

### 操作系统的体系结构

两种体系结构：大内核、微内核（不同内核划分）
<div style="display: flex; width: 100%; margin-left: 0;">
  <div style="flex: 65; text-align: center;">
    <img src="/images/kernelBig.png" alt="分数构成" style="width: 100%; max-width: 800px; height: auto;">
  </div>
  <div style="flex: 35; text-align: center;">
    <img src="/images/kernelMicro.png" alt="分数构成" style="width: 100%; max-width: 800px; height: auto;">
  </div>
</div>

| 体系结构 | 优点              | 缺点                   |
|------|-----------------|----------------------|
| 大内核  | 高性能，用户态和内核态切换少  | 内核代码庞大，结构混乱，难以维护     |
| 微内核  | 内核功能少，结构清晰，方便维护 | 需要频繁在核心态和用户态之间切换，性能低 |

---

### 操作系统引导-开机过程

<span id="操作系统引导""></span>
开机过程
![img_19.png](img_19.png)
---

### 虚拟机
- 物理机器虚拟化为多台虚拟机(VM)，需要使用虚拟机管理程序/虚拟机监控程序(VMM/Hypervisor)

